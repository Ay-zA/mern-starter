import React from 'react';
import './About.scss';

const About = () => (
  <div>
    <h1 className="title">
      Hello from
      <span>React + Mongo + Express + Node + Webpack + HMR + ES7 + ESLinit + GraphQL</span>
    </h1>
  </div>
);

export default About;
